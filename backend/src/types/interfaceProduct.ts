export interface interfaceProduct extends Document {
    //category?: String,
    nameProduct: String,
    description: String,
    price: Number,
    active: Boolean,

    // add seed categorys predefinidas
}
