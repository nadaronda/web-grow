export interface interfaceProduct {
    //category?: String,
    _id?: string,
    nameProduct: string,
    description: string,
    priceVentaClient: string,
    priceCompra: string,
    active?: boolean,

    // add seed categorys predefinidas
}