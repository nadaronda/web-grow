export interface interfaceProduct {
    //category?: String,
    _id?: string,
    nameProduct: string,
    description: string,
    priceVentaClient: number,
    priceCompra: number,
    active?: boolean,

    // add seed categorys predefinidas
}